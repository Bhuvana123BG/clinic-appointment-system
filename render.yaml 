services:
  - type: web
    name: django-template-app
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: myproject.settings  # 👈 Replace with your actual project name
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: false
